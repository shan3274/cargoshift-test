'use client';
import Logo from './Logo';
import { useState } from 'react';
import Link from 'next/link';
import clsx from 'clsx';
const links = [ { href: '#features', label: 'Features' }, { href: '#how-it-works', label: 'How it Works' }, { href: '#contact', label: 'Contact' } ];
export default function NavBar() { const [open, setOpen] = useState(false); return ( <header className="sticky top-0 z-40 backdrop-blur bg-white/70 border-b border-gray-100"> <div className="mx-auto max-w-7xl px-4 sm:px-6 py-3 flex items-center justify-between"> <Logo /> <nav className="hidden md:flex gap-8 text-sm font-medium"> {links.map(l => ( <a key={l.href} href={l.href} className="text-gray-600 hover:text-gray-900"> {l.label} </a> ))} </nav> <div className="hidden md:flex items-center gap-3"> <Link href="#contact" className="inline-flex items-center rounded-md bg-brand-600 px-4 py-2 text-white text-sm font-semibold shadow hover:bg-brand-500 transition"> Request Demo </Link> </div> <button onClick={() => setOpen(o => !o)} className="md:hidden inline-flex items-center justify-center rounded-md p-2 text-gray-600 hover:bg-gray-100" aria-label="Toggle navigation"> <svg className="h-6 w-6" fill="none" stroke="currentColor"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M4 6h16M4 12h16M4 18h16"/> </svg> </button> </div> <div className={clsx( 'md:hidden transition-all overflow-hidden', open ? 'max-h-96 border-t border-gray-100' : 'max-h-0' )}> <div className="px-4 pb-4 flex flex-col gap-2"> {links.map(l => ( <a key={l.href} href={l.href} onClick={() => setOpen(false)} className="py-2 text-gray-700 hover:text-gray-900"> {l.label} </a> ))} <a href="#contact" className="mt-2 inline-flex items-center justify-center rounded-md bg-brand-600 px-4 py-2 text-white text-sm font-semibold shadow hover:bg-brand-500 transition" onClick={() => setOpen(false)}> Request Demo </a> </div> </div> </header> ); }